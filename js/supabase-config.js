/* ================================================================
   5 METRE SCRUM — SUPABASE CONNECTION SETTINGS
   Paste your two values from Supabase → Project Settings → API:
     SUPABASE_URL       the "Project URL", e.g. https://abcdefgh.supabase.co
     SUPABASE_ANON_KEY  the "anon public" key (a long string starting eyJ...)

   The anon key is designed to be public, so it is safe in site code.
   Never paste the "service_role" key here.

   While these are left blank the whole site runs in DEMO MODE:
   sign-ups, rosters, vacancies and staff profiles are saved in the
   visitor's own browser only.
   ================================================================ */
window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
